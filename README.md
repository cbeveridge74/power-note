# Powernote

## Overview
Small React App investigation (~2021) the use of setting the `contentEditable="true"` of an HTML Element with the goal of using it to create a powerful editing component.

### Usage

```
npm i
npm run dev
```


